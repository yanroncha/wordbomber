# WordBomber 仕様書

ゼビウス風の縦スクロールシューティング。敵機との空中戦はなく、**地上砲台への爆撃のみ**。各砲台には英字が刻まれており、**画面上部の「お題単語」の綴り順に爆撃すると得点**になる。難度が上がると、お題が**虫食い(一部が `?` で隠される)**になり、プレイヤーは単語を推理しながら爆撃する。

- GitHub: <https://github.com/yanroncha/wordbomber>(origin / master)
- 公開: GitHub Pages <https://yanroncha.github.io/wordbomber/>(master ルート、push で自動再公開)
- Artifacts 公開用の単一HTML: `node build.js` → `dist/wordbomber.html`

## 技術方針

- 素の JavaScript + Canvas 2D(480×640)、依存ライブラリゼロ、画像・音声アセットなし(図形描画+WebAudio合成)
- ES modules 不使用(`file://` 直開き対応)。`<script>` 順次読み込み、グローバル `WB` 名前空間に集約
- 入力は `WB.Input`(down フラグ+1フレーム edge)に一元化。タッチUIは同じフラグに仮想キーを注入

## ゲーム仕様

### 操作

| 操作 | PC | タッチ(スマホ) |
|---|---|---|
| 移動(上下左右) | ←→↑↓ / WASD | 画面ドラッグ(指の移動量×1.45の相対移動) |
| 爆撃 | SPACE / Z | 右下💣ボタン(長押しで連射) / 移動中に2本目の指でタップ |
| ポーズ | P / ESC | 右上⏸アイコン |
| ミュート切替 | M | ⏸下のスピーカーアイコン |
| 開始/リトライ | SPACE / ENTER | 画面タップ |

- **PC/スマホ動的切替**: 初期判定は `pointer: coarse`。以後タッチで表示・キー押下で非表示。キーボードは常時有効
- 移動範囲: 左右は全幅、上下は画面中央(35%)〜下部。自機・照準とも画面外に出ない
- 左右移動時は**バンク表現**(ロール+横圧縮)。ドラッグ操縦でも連動
- 機体は**ミレニアムファルコン風**(円盤+双胴マンディブル、右舷コックピット、レーダーディッシュ、エンジン発光)

### 爆撃と単語判定

- 自機前方130pxの照準に約0.4秒後着弾(偏差爆撃)。同時2発・クールダウン0.28秒
- お題の綴り順に文字砲台を爆撃で1文字確定。**誤爆=ミス**: 進捗リセット+機体1機減
- **候補集合判定**: 虫食い位置は「開示済みパターン+爆撃済み文字に合致する辞書単語が1つでもあれば正解」(例: `C?T` は CAT / CUT どちらも可)。開示済み文字はミス後も維持
- 無地バンカーは小得点のみ・ペナルティなし
- スポーナーが「候補単語の次に必要な文字」を保証供給(詰みなし)

### 敵の攻撃と機体

- 地上砲台が自機を狙って低速弾で反撃。**フィールド上の同時砲弾は最大5発**
- 被弾で機体1機減(**初期8機**、0でゲームオーバー)+爆発演出。被弾後約1.6秒無敵(点滅)
- **単語完成時、飛行中の砲弾は全消去**

### 単語完成・レベル進行

- 単語完成時: **日本語訳バナー**(緑リング+パネル)+ファンファーレ(C-E-G-C)+爆発バースト+**地形バイオーム切替**
- 3単語完成ごとにレベルアップ。単語長 3→最大7文字、虫食い数増加(Lv3以降、最大で長さ−2)、スクロール・砲撃頻度も上昇
- **レベルアップ時はワープ演出(約2秒)**: スクロール×5+縦スピードライン+機体点滅(無敵)+砲台射撃停止+上昇スイープ音

### スコア

- 正解1文字: 100×LV(虫食い位置は×2)
- 単語完成: 500×文字数×LV×コンボ倍率(ミスなし連続完成で+0.5ずつ、最大×3)
- **1000点ごとに残機+1**(上限なし、「1UP!」表示)。得点加算は1本のヘルパーに集約
- 残機HUDは6機まではミニ機体アイコン、7機以上は「▲×N」の数値表示
- ハイスコアは localStorage に保存(使えない環境では保存のみ無効)

### ヘルプアイテム

- 金属プレートにアイコンを描いた落下アイテム(機体よりやや小)。スクロールより速く落下し、機体接触で発動。爆撃対象外・取り逃し(画面外)で消滅
- 同時最大3個、8〜15秒間隔でずらして出現(初回は3〜6秒後)
- 4種:
  - **無敵▲**(太い上向き矢印): 30秒間、点滅しながら砲撃を無効化
  - **砲弾減速↘**(斜め下カーブ矢印): 30秒間、砲弾速度を半減
  - **砲弾殲滅⚡**(稲妻): フィールド上の砲弾を即時一掃
  - **1文字💡**(点灯ランプ): お題の現在対象を1文字正解
- **持続効果(無敵/減速)は排他1枠**: 後から取った方が効果・残時間を上書き(30秒にリセット)。HUD左上に「効果名 + 残り秒数」を表示

### ボス戦

- **レベルが10の倍数に達するごと**にボス戦へ突入(ワープの代わり)。突入中はフィールドのスクロールが停止
- ボスは**デススター風の巨大円形要塞**(半径150)。上部からゆっくり降下し、前後左右にゆっくり揺動
- 表面に**6砲台**。地上砲台と同様に自機を狙って砲撃(全体の砲弾上限5は共通)。文字の爆撃効果は通常と同一
- 爆撃された砲台は**点滅→約2.6秒後に新しい文字で復活**。常に必要文字がいずれかの砲台に出るよう保証(詰み防止)
- ボスの揺動は、**砲台が最下部の自機照準(y≈460)より下に来ない**よう制限(実際は y≤約280 で十分上方)
- **5単語の完成で勝利**: ボス上に爆発アニメーション(効果音付き)、ボスが点滅して消滅、500点加算。撃破後はスクロール再開・地上砲台復帰
- ボス戦中もお題攻略・得点・残機+1・ヘルプアイテム・レベルアップのルールは不変(ワープ演出のみ抑制)

### フィールド

- **バイオーム帯**(森=樹冠3層の木々/平地=草・畑・茂み/砂漠=砂丘・サボテン・岩/海=動く波・小島)を境界グラデーションで連結
- バイオームは**1ワード完成ごとに切替**(プレイ中は固定)。同じ地形は連続しない

### サウンド(WebAudio合成)

- BGM: 「ピロピロ高→ピロピロ低」8音ループ(0.16秒/音)の単調なメロディー
- SFX: 砲台破壊音、被弾音、単語完成ファンファーレ、ワープ音
- 初回のキー入力**またはタッチ**で有効化(ブラウザの自動再生制限対応)
- M キーまたはスピーカーアイコンでミュート切替(ミュート中は画面左下に表示)。iPhone の消音スイッチON時は端末側で音が抑制されることがある

### 画面遷移

タイトル(操作説明はPC/タッチで切替) → プレイ → **ポーズ(盤面完全非表示=カンニング防止。再開 / リスタート可)** → ゲームオーバー(リトライ)

## ファイル構成

```
index.html          エントリポイント(script依存順読み込み)
css/style.css       スタイル+touch-action等のモバイル対応
js/words-data.js    辞書421語(3〜7文字、全語に日本語訳) WB.DICT/WB.WORDS/WB.translate
js/input.js         キー入力+仮想キー注入 press()
js/touch.js         タッチUI(ドラッグ操縦/💣/⏸/🔊、動的切替)
js/background.js    バイオーム地形+changeBiome()
js/word.js          お題・虫食い・候補集合判定
js/spawner.js       砲台スポーン(必要文字保証)
js/turret.js        砲台(反撃タイマー付き)
js/player.js        自機(moveBy/バンク/被弾無敵/ワープ点滅)
js/bomb.js          爆弾・爆発パーティクル
js/enemyfire.js     反撃弾(上限5・clear()・速度係数)
js/items.js         ヘルプアイテム(落下・接触・4効果アイコン)
js/boss.js          ボス戦(円形要塞・6砲台・降下/揺動/撃破)
js/audio.js         BGM・SFX・ミュート
js/hud.js           スコア・お題・機体数・訳バナー
js/game.js          状態機械・スコア・ワープ・getState()
js/main.js          ゲームループ
build.js            単一HTML生成(node build.js → dist/wordbomber.html)
```

## 検証方法

- ローカル: `python -m http.server 8734` 等で起動(`file://` 直開きも可)
- オートパイロット(合成キーイベント)で正解爆撃/誤爆/完成/レベルアップを駆動し、スコア・残機・バナー・ワープ・砲弾クリアを確認
- タッチは合成 TouchEvent でドラッグ移動量・ボタン類・動的切替を確認(非表示タブでは rAF 停止と rect=0 に注意 → 手動フレームポンプ+ピクセル検査で代替)
- `file://` 直開き、`dist/wordbomber.html` 単体、GitHub Pages 公開版の3形態で動作確認
